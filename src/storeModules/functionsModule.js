import firebase from '../firebaseConfig'
let fbFunctions = firebase.fbFunctions;

const state = {}
const getters = {}
const mutations = {}
const actions = {}

export default {
    actions,
    mutations,
    getters,
    state
}